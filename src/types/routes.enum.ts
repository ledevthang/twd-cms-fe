export enum AuthRoutesEnum {
  login = '/auth/login',
  resgister = '/auth/resgister',
  renew = '/auth/access-token'
}
export enum ApiRoutesEnum {
  crud = '/crud',
  empty = '/empty',
  notFound = '/notfound',
  chart = '/charts',
  accessDenied = '/access',
  table = '/table'
}
